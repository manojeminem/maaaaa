#include<stdio.h>
#include<conio.h>
void main()
{
int i,a,b,c;
clrscr();
printf("\n the value of :");
scanf("%d%d",&a,&b);
c=(a/b==1);
for(i=0;i<b,i++)
if (i%==0)
{
printf("\n the value is%d,i);
}
else
{
printf("\n the value is %d,c);
}
getch();
}
